import Foundation

struct Constants {
    static let apiBaseURL = "https://aviationweather.gov/data/api/metar"
    static let refreshInterval: TimeInterval = 600 // 10 minutes
    static let menuTitle = "Aviation METARs"
    static let configMenuTitle = "Config"
    static let refreshNowTitle = "Refresh Now"
    static let quitTitle = "Quit"
}