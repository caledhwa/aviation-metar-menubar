import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Aviation Metar Menubar")
            .frame(width: 200, height: 100)
            .padding()
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}